// 1.You can't define a variable more than once
let petName = 'Hal'

petName = "Spot" //if we did let petName = "spot" then that would be invalid

// 2, There are rules related to variable names
let test_ = 2 //the variable name cannot start with a number but can contain numbers

//3. There are reserved keywords that can't be used

let let = 2 //This is invalid